machine:
  node:
    version: 4.3.2

deployment:
  test:
    branch: master
    owner: Financial-Times
    commands:
      - make deploy-test
  production:
    tag: /prod-.*/
    owner: Financial-Times
    commands:
      - make deploy-prod
